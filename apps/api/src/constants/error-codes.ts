/**
 * رموز أخطاء ثابتة — للحالات التي تتفرّع عندها الواجهة **سلوكياً** لا عرضياً
 * (شاشة مختلفة، تلوين حقل بعينه). ليست بديلاً عن الرسالة: الرسالة للمستخدم
 * والرمز للبرنامج.
 *
 * سبب وجودها: كانت الواجهة تطابق على نصّ الرسالة الإنجليزي حرفياً، فأسقط
 * تعريبُ الرسائل تلك الفروع بصمت. النصّ يتغيّر مع أي تحسين صياغة، والرمز لا.
 *
 * لا تُضف رمزاً إلا لحالة تحتاجها الواجهة فعلاً — قائمة قصيرة مقصودة.
 */
export const ERROR_CODES = {
  /** رابط إعادة تعيين كلمة السر غير صالح أو منتهٍ ⇒ شاشة «اطلب رابطاً جديداً» */
  INVALID_RESET_TOKEN: "INVALID_RESET_TOKEN",
  /** كلمة السر الحالية خاطئة ⇒ تعليم حقل «كلمة السر الحالية» دون غيره */
  WRONG_CURRENT_PASSWORD: "WRONG_CURRENT_PASSWORD",
  /**
   * الخادم حيّ لكنه لا يبلغ قاعدة البيانات ⇒ طبقة التوجيه بسطح المكتب تُحوِّل
   * الطلب للمخزن المحلّي. كانت تستدلّ على ذلك بالبحث عن «قاعدة البيانات» داخل
   * نصّ الرسالة — وهو ما يجعل تحسين صياغة واحد كفيلاً بتعطيل الأوفلاين صامتاً.
   */
  DB_UNREACHABLE: "DB_UNREACHABLE",
} as const;

export type ErrorCode = (typeof ERROR_CODES)[keyof typeof ERROR_CODES];
